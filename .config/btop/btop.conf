#? Config file for btop

# Show only CPU and memory boxes (hide proc and net)
shown_boxes = "cpu mem"

# Single aggregated CPU graph instead of per-thread
cpu_single_graph = True

# Update interval in milliseconds
update_ms = 2000

# Vim-style key bindings
vim_keys = True

# Color theme
color_theme = "TTY"
